<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.nflAppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/dark_background</item>
        <item name="colorPrimaryVariant">@color/dark_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary_accent</item>
        <item name="colorSecondaryVariant">@color/secondary_accent_variant</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- Button Style -->
    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <!-- Set the background color of the button -->
        <item name="android:backgroundTint">@color/secondary_accent</item>

        <!-- Set the text color of the button -->
        <item name="android:textColor">@color/white</item>

        <!-- Set the ripple effect color -->
        <item name="rippleColor">@color/secondary_accent_variant</item>
    </style>
</resources>
